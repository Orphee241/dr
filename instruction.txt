# Creer le fichier docker-compose.yml qui permet de lancer le projet dynamiqueregionale en local
# Creer le fichier Dockerfile qui permet de builder l'image docker
# Ajoute mysql et phpmyadmin a docker-compose.yml
# utiliser le port 8001 pour mon application s'i est disponible

